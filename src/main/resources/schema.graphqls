type Artist {
    id : ID!
    name: String
}

type Song {
    id: ID!
    name: String
    artist: Artist
}

# The Root Query for the application
type Query {
    artists: [Artist]
    songs: [Song]
}
